\documentclass[a4paper,10pt]{article}
\usepackage[utf8]{inputenc} % Enables UTF-8 encoding
\usepackage[T1]{fontenc}         % Correct font output for accents
\usepackage{textcomp}            % Extended symbols
\usepackage{lmodern}             % Modern font that supports most glyphs
\usepackage[left=1in, right=1in, top=1in, bottom=1in]{geometry}
\usepackage{titlesec}
\usepackage{enumitem}
\usepackage{paralist}
\usepackage{hyperref}
\usepackage{xcolor}
\usepackage{helvet}
\usepackage{mathptmx} % Times New Roman
\titleformat{\section}{\large\bfseries}{\thesection}{1em}{}[\titlerule]
\titleformat{\subsection}{\bfseries}{\thesubsection}{1em}{}

\begin{document}

% Header Section
\begin{center}
    {\huge \textbf{ {{ name }} }} \\
    \vspace{5pt}
    \small \href{mailto:{{ email }} } {{ email }} \quad \textbullet{} {{ phoneNumber }}   {% for profile in socialProfiles %}  
    \quad \textbullet{} \href{ {{ profile.link }} }{{profile.name}} {%endfor%}
  
\end{center}

% Education Section
\section*{Education}
{% for edu in education %}
\noindent
\textbf{ {{ edu.degree }} in {{edu.branch}} } \hfill  \textit{ CGPA: {{ edu.cgpa }} } \\
{{edu.institute}}  \hfill  \textit{ {{edu.endYear}} } 
\vspace{0.3cm}
{% endfor %}

% Projects Section
{% if 'projects' in selectedSections and projects %}
\section*{Projects}
\noindent
\begin{compactitem}
    {% for project in  projects %}
        \item\textbf{ {{ project.title }} } \hfill \textit{ Tech Stack: {{ project.techStack }} } 
        \begin{compactitem}
            {% for point in project.description %}
            \item {{point}}
            {% endfor %}
        \end{compactitem}
        \href{ {{ project.link }} }{project link}
    {% endfor %}
\end{compactitem}
{% endif %}

% Skills Section
{% if  skills %}
\section*{Skills}
\noindent
\begin{compactitem}
    {% for skill in skills %}
        \item {{ skill}}
    {% endfor %}
\end{compactitem}
{% endif %}

%Experience Section
{% if 'experience' in selectedSections and experience %}
\section*{Experience}
\noindent
\begin{compactitem}
    {% for exp in experience %}
    \item \textbf{ {{exp.role}} in {{exp.company}} }  ( {{exp.duration}} )  \\
     {{exp.description}}
    {%endfor%}
\end{compactitem}
{% endif %}

% Certifications Section
{% if 'certifications' in selectedSections and certifications %}
\section*{Certifications}
\noindent
\begin{compactitem}
    {% for cert in certifications %}
        \item {{ cert.name }} \hfill \textit{ {{ cert.date }} }
    {% endfor %}
\end{compactitem}
{% endif %}

\end{document}
